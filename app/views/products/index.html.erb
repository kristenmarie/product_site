<h1>Welcome to Mario's Foods!</h1>

<ul>
  <% @products.each do |product| %>
    <li><%= link_to product.name, product_path(product), class: 'product' %> | <button class="btn btn-success"><%= link_to "Edit", edit_product_path(product) %></button> </li>
  <% end %>
</ul>
