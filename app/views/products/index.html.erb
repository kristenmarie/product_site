<h1>Welcome to Mario's Foods!</h1>

<ul>
  <% @products.each do |product| %>
    <li>  <%= link_to product.name, product_path(product) %> | <%= link_to "Edit", edit_product_path(product) %> </li>
  <% end %>
</ul>
